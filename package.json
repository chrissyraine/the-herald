{
  "name": "theherald",
  "version": "1.0.0",
  "description": "Content Syndication Layer for TownSquare",
  "scripts": {
    "dev": "wrangler pages dev public",
    "build": "echo 'No build step needed'",
    "deploy": "wrangler pages deploy public",
    "db:init": "wrangler d1 execute herald --local --file=./schema.sql",
    "test": "vitest run"
  },
  "type": "module",
  "dependencies": {
    "wrangler": "latest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
